// Code generated by github.com/kjkrol/goke/internal/cmd/gen/specs; DO NOT EDIT.
package comp

import (
	"fmt"
	"reflect"
)

{{ range $n := .Range }}
// Spec{{ $n }} interns {{ $n }} component type(s) and builds the component mask.
type Spec{{ $n }}{{ typeList $n }} struct {
	Blueprint
	{{ range $i := (seq 1 $n) -}}
	Meta{{ $i }} Meta
	{{ end }}
}

// Init interns T1..T{{ $n }} via c, builds the inclusion mask, and applies opts.
// Panics on duplicate component types or conflicting opts.
func (s *Spec{{ $n }}{{ typeArgs $n }}) Init(c *Catalog, opts ...BlueprintOpt) {
	{{ range $i := (seq 1 $n) -}}
	s.Meta{{ $i }} = c.Intern(reflect.TypeFor[T{{ $i }}]())
	if err := s.Blueprint.Comp(s.Meta{{ $i }}); err != nil {
		panic(fmt.Sprintf("comp: Spec{{ $n }}.Init: %v", err))
	}
	{{ end -}}
	for _, opt := range opts {
		if err := opt(&s.Blueprint, c); err != nil {
			panic(fmt.Sprintf("comp: Spec{{ $n }}.Init option: %v", err))
		}
	}
}
{{ end }}
